openapi: 3.0.3
info:
  title: Exuviae Hub API
  version: "0.1.0"
servers:
  - url: http://localhost:8000

paths:
  /api/v0/nodes/register:
    post:
      summary: Register or update a node and its capabilities
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/NodeRegister"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OkResponse"

  /api/v0/nodes:
    get:
      summary: List registered nodes (v0.1 basic management)
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NodeListResponse"

  /api/v0/capture:
    post:
      summary: Request a snapshot capture from a node
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CaptureRequest"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CaptureResponse"

  /api/v0/snapshots/upload:
    post:
      summary: Upload a snapshot image (multipart)
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/SnapshotUploadForm"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SnapshotUploadResponse"

  /ws/v0:
    get:
      summary: WebSocket endpoint (upgrade)
      description: >
        WebSocket upgrade endpoint. Client should provide node_id as query string, e.g. /ws/v0?node_id=cam-01
      responses:
        "101":
          description: Switching Protocols

components:
  schemas:
    OkResponse:
      type: object
      required: [ok]
      additionalProperties: true
      properties:
        ok:
          type: boolean
          enum: [true]

    SnapshotUploadResponse:
      type: object
      required: [ok, image_path]
      additionalProperties: true
      properties:
        ok:
          type: boolean
          enum: [true]
        image_path:
          $ref: "#/components/schemas/ImagePath"

    NodeRegister:
      type: object
      required: [node_id, kind, firmware, capabilities]
      additionalProperties: true
      properties:
        node_id:
          $ref: "#/components/schemas/NodeId"
        kind:
          type: string
          minLength: 1
          maxLength: 64
        firmware:
          type: string
          minLength: 1
          maxLength: 64
        capabilities:
          type: object
          additionalProperties: true

    NodeInfo:
      type: object
      required: [node_id, kind, firmware, capabilities]
      additionalProperties: true
      properties:
        node_id:
          $ref: "#/components/schemas/NodeId"
        kind:
          type: string
        firmware:
          type: string
        capabilities:
          type: object
          additionalProperties: true
        last_seen_ts:
          type: string

    NodeListResponse:
      type: object
      required: [ok, nodes]
      additionalProperties: true
      properties:
        ok:
          type: boolean
          enum: [true]
        nodes:
          type: array
          items:
            $ref: "#/components/schemas/NodeInfo"

    CaptureRequest:
      type: object
      required: [node_id]
      additionalProperties: true
      properties:
        node_id:
          $ref: "#/components/schemas/NodeId"

    CaptureResponse:
      type: object
      required: [ok, snapshot_id]
      additionalProperties: true
      properties:
        ok:
          type: boolean
          enum: [true]
        snapshot_id:
          $ref: "#/components/schemas/SnapshotId"

    SnapshotUploadForm:
      type: object
      required: [node_id, snapshot_id, image]
      additionalProperties: true
      properties:
        node_id:
          $ref: "#/components/schemas/NodeId"
        snapshot_id:
          $ref: "#/components/schemas/SnapshotId"
        meta:
          type: string
          description: Optional JSON string metadata
        image:
          type: string
          format: binary

    NodeId:
      type: string
      minLength: 1
      maxLength: 64
      pattern: '^[a-zA-Z0-9][a-zA-Z0-9._-]{0,63}$'

    SnapshotId:
      type: string
      minLength: 1
      maxLength: 128
      pattern: '^s-[0-9]{8}-[0-9]{6}-[0-9a-f]{8}$'

    ImagePath:
      type: string
      minLength: 1
      maxLength: 512
      pattern: '^data/snapshots/[0-9]{4}-[0-9]{2}-[0-9]{2}/[a-zA-Z0-9][a-zA-Z0-9._-]{0,63}/s-[0-9]{8}-[0-9]{6}-[0-9a-f]{8}\.jpg$'

